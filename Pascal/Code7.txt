PROGRAM Main;
   VAR x, y: REAL;

   PROCEDURE Alpha(a,b,c : INTEGER; d:REAL; e, f:REAL);
      VAR y : INTEGER;
   BEGIN
      x := a + x + y;
   END;

   PROCEDURE AlphaB(a, b : INTEGER);
      VAR y : INTEGER;
   BEGIN
      x := y;
   END;

BEGIN

END. 