PROGRAM Main;
   VAR x, y: REAL;

   PROCEDURE AlphaB(a, b : INTEGER);
      VAR y : INTEGER;
      PROCEDURE AlphaC(a : INTEGER; c, z : REAL; d : REAL);
        VAR y : REAL;
      BEGIN
        y := x;
      END;
   BEGIN
      x := y;
   END;

BEGIN

END. 